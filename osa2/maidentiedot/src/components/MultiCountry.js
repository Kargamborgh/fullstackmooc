import React from 'react'

const MultiCountry = ({ length, filterValue, country }) => {

    console.log(country)

    //    console.log(length)

            return (
                <li>
                    {country.name}
                </li>
                )
            }


export default MultiCountry